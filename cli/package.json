{
  "name": "@zeroshot/cli",
  "version": "0.1.0",
  "description": "ZeroShot production CLI wrapper",
  "type": "module",
  "bin": {
    "zeroshot": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "@openai/codex-sdk": "^0.129.0",
    "commander": "^14.0.1",
    "zod": "^3.25.76"
  }
}
